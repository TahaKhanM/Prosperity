interface:
  display_name: "Competition Research Agent"
  short_description: "Official docs, current context, stale-assumption checks"
  default_prompt: "Use the Competition Research Agent skill to verify official current Prosperity assumptions, identify stale beliefs, and write a current assumptions note."
policy:
  allow_implicit_invocation: false
