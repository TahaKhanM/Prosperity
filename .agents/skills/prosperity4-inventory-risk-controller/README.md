# Prosperity 4 Inventory Risk Controller

Audits the *state trajectory* of trader variants — position path, loss clusters, drawdowns, tail events — not just headline PnL.

Suggested invocation:
- "Use prosperity-4-inventory-risk-controller to audit run `<id>` for tail-dominated PnL and limit-pin events."
