# VEV_4000 — Round 4 per-product lens battery

## Headline numbers
- Mids: d1=1248.4, d2=1255.4, d3=1239.2; std 14.6/17.0/18.6.
- Spread median **21** every day, IQR collapsed. VE spread = **5**. VE is 4x cheaper to cross.
- Trades: 164/128/150. Mark14+Mark38 ≈90% (paired buy/sell).
- TV: mean ≈0.005, median 0, std 0.83-0.91 — delta-1 floor with 0.5-tick noise.
- **Same-tick β vs ΔVE per day: d1=0.758, d2=0.733, d3=0.734** (R²≈0.31-0.36). Theoretical=1.0; empirical β<1 is attenuation bias from voucher rounding noise (std(ΔV)/std(ΔS) = 1.24-1.32 — see Lens F).

## Lens A — what's surprising
Median spread is exactly 21 with IQR=[21,21] — a single quoter pegs ±10.5 around mid every tick. Range ~85 ticks per day. No crossed/locked books.

## Lens B — what's surprising
ρ1 = −0.286 / −0.286 / −0.309 (consistent across days). OU half-life 145–215 ticks. Variance ratio at k=200 = 0.36-0.44 — strong mean-reversion at multi-second scale. Hurst 0.43-0.47.

## Lens C — what's surprising
imb1 → Δmid_{t+1} β ≈ +9.7 / t≈+55. imb3 → Δmid β ≈ −95 / t≈−55: level-3 book pressure inverts. Wall-mid β ≈ 0.91 (t > 60) — wall-mid is the dominant predictor.

## Lens D — what's surprising
Mark 14 / Mark 38 per-tick conditional Δmid is **<|0.3| / unit, t < 2** at h=1,5,20. The +10/unit "alpha" is the entry-to-100tick-VE drift, not actionable per-tick. Mark 22 trades = noise (n=1).

## Lens E (cross-asset with VE) — KEY
- Same-tick ρ(ΔV_4000, ΔVE) = +0.600 / +0.590 / +0.556. Lower than R3's 0.62 — voucher idiosyncratic noise dominates 1-tick moves.
- Lead-lag |ρ| ≤ 0.05 at k=±1,±2,±3,±5. **No tradeable lead-lag.**
- **Residual AC1 (after stripping β·ΔVE): −0.469 / −0.487 / −0.493 — very strong.** Voucher mid over-reacts vs theoretical (S-K) and snaps back next tick.

## Lens F (weird)
std(ΔV_4000)/std(ΔVE) = 1.27 / 1.24 / 1.32 — voucher mid is *noisier* than VE mid in absolute terms despite β<1. Reason: 0.5-tick rounding and quote-bouncing on the 21-wide book. Spectral peaks at 2000 (cosmetic ramp at k=2).

## Candidate alphas
- **R4-V4000-E01: β-residual fade.** Sign(V_4000_mid − (VE_mid + 4000) − μ_drift) → fade. AC1 of residual ≈ −0.48. Mag = the residual std times AC1 ≈ 0.48 × 0.6 ≈ 0.3 ticks/decision; on 21-wide book, only realisable if VE side beats. **Falsifier**: backtest in shadow mode — net edge after a 21-spread cross must be > 0 (it almost certainly is not).
- **R4-V4000-G01: synthesise short VE via V_4000 ask?** No: VE spread (5) << V_4000 spread (21), so V_4000 is *worse* execution. Confirmed: parity violations 0/0/0 (4000+v4_ask < ve_bid never; opposite never). **Reject.**

## Rejected here
- IV scalp: TV ≈ 0, IV solver returns 0; no smile to scalp.
- Lead-lag k=−1: |ρ|=0.029, t < 3 at n=10k — economically zero, not worth crossing 21-wide book.
- Mark 14 follow on V_4000: per-tick Δmid t-stats < 2; signal lives in VE space, not V_4000 mid.
- Tradeable parity arb (G1 on V_4000): 0/1/2 violations × 1-tick edge / 10k = ~ε.
