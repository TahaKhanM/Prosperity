# 06_validation/baselines

Purpose: define and snapshot the baselines that candidate traders must beat.

Expected inputs: benchmark trader references, historical validation notes, and
baseline metrics from `prosperity_rust_backtester/`.

Expected outputs: dated baseline summaries that downstream comparison notes can
cite directly.

Naming conventions: use `YYYY-MM-DD_<baseline>.md`.

Classification: canonical stage artifact directory.
