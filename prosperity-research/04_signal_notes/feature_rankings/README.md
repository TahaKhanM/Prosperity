# 04_signal_notes/feature_rankings

Purpose: summarize which features appear most promising before implementation.

Expected inputs: feature tables, notebook outputs, and per-product EDA notes.

Expected outputs: ranked feature summaries and rolling ranking snapshots.

Naming conventions: use `YYYY-MM-DD_<topic>.md` and `latest_<topic>.md` only
for intentionally maintained rolling summaries.

Classification: canonical stage artifact directory.
