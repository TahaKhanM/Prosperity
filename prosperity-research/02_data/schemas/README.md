# 02_data/schemas

Purpose: define expected schema contracts for normalized data and feature
artifacts.

Expected inputs: artifact-family field definitions and compatibility notes.

Expected outputs: schema files referenced by manifests, notebooks, and
validation tooling.

Naming conventions: use `<artifact_family>.schema.yaml`.

Classification: canonical stage artifact directory.
