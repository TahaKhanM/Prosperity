# 02_data/features

Purpose: store feature tables derived from normalized datasets.

Expected inputs: normalized Parquet datasets, feature definitions and schema or
manifest references.

Expected outputs: feature datasets ready for EDA, ranking and validation
support.

Naming conventions: use `<dataset>__<feature_set>.parquet`.

Classification: canonical stage artifact directory.
