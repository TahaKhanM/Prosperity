# 02_data/manifests

Purpose: record artifact metadata, provenance, and generation details for data
and feature outputs.

Expected inputs: source descriptions, generation parameters, and dataset or
feature lineage details.

Expected outputs: YAML manifests that let downstream work trace how an artifact
was produced.

Naming conventions: use `<artifact_name>.manifest.yaml`.

Classification: canonical stage artifact directory.
