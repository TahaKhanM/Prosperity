# 05_execution_risk/inventory

Purpose: diagnose inventory path, limit pressure, carry behavior, and
flattening failures after validation.

Expected inputs: persisted run artifacts, position traces, and validation notes.

Expected outputs: inventory-specific diagnosis notes and supporting figures.

Naming conventions: use `YYYY-MM-DD_<topic>.md` and
`YYYY-MM-DD_<topic>_<view>.png`.

Classification: canonical stage artifact directory.
